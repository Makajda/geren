namespace Geren.Map;

internal static class SchemaTypeMetadata {
    internal static string Resolve(string dotnet_type, Compilation compilation, ImmutableArray<Diagnostic>.Builder diagnostics) {
        return "object";//todo
    }
}
